public Integer fibonacci(Integer n) {
    //code here
    if(n<=2)
    return 1;
    Integer curr, prev = 1;
    for(Integer i=3; i<=5;i++){
        Integer temp = curr;
        curr = curr+prev;
        prev = temp;
    }
    return curr;
}

